#!/bin/bash

source ./ascii.sh
echo ''

if [ $# -eq 0 ]; then
	SUB=$(gum choose "Font" --height 4 --header "" | tr '[:upper:]' '[:lower:]')
	# SUB=$(gum choose "Theme" "Font" "Install" "Update" --height 8 --header "" | tr '[:upper:]' '[:lower:]')
else
	SUB=$1
fi

[ -n "$SUB" ] && source ./$SUB
